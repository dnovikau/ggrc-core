{{!
  Copyright (C) 2019 Google Inc.
  Licensed under http://www.apache.org/licenses/LICENSE-2.0 <see LICENSE file>
}}

<select class="{{firstnonempty className 'input-block-level'}} {{#is(name, 'instance.status')}}status{{/is}}"
        can-value="{{name}}" can-change="onChange"
        null-if-empty="null-if-empty" tabindex="{{tabIndex}}"
        {{#isDisabled}}disabled="disabled"{{/isDisabled}}
        {{#controlId}}id="{{controlId}}"{{/controlId}}>
  {{#each options}}
    {{#if ./title}}
      <option value="{{value}}">{{title}}</option>
    {{/if}}
    {{#if group}}
      <optgroup label="{{group}}">
      {{#subitems}}
        <option value="{{value}}">{{title}}</option>
      {{/subitems}}
      </optgroup>
    {{/if}}
  {{/each}}
</select>
